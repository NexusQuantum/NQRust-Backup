Console {
   Name = "limited-operator"
   Password = "secret"
   TLS Enable = no

   Profile = "operator"

   Command ACL = "!.consoles"
   Where ACL = "/.*/tmp/nqrustbackup-restores-limited-operator", "!*all*"
}
