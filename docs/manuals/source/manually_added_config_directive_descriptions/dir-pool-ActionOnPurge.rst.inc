The directive ``Action On Purge=Truncate`` instructs NQRustBackup to truncate the volume when it is purged with the :bcommand:`purge volume action=truncate` command. It is useful to prevent disk based volumes from consuming too much space.
