The backup format used for protocols which support multiple formats. By default, it uses the NQRustBackup :strong:`Native` Backup format. Other protocols, like NDMP supports different backup formats for instance:

-  Dump

-  Tar

-  SMTape
